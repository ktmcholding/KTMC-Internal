[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app fallback: serve index.html for client-side routes
# (existing static files in /dist are served first).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
